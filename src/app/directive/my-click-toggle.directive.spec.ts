import { MyClickToggleDirective } from './my-click-toggle.directive';

describe('MyClickToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new MyClickToggleDirective();
    expect(directive).toBeTruthy();
  });
});
